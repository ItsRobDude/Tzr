{
  "name": "engine-wasm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:wasm": "wasm-pack build --target nodejs --out-dir pkg --release",
    "test": "npm run build:wasm && tsx --test tests/simulate_wave.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "tsx": "^4.15.7",
    "typescript": "^5.4.5"
  }
}
